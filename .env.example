# Environment Configuration
# Copy this file to .env and fill in your actual values

# Frontend Configuration
# Set to "true" to use mock data, "false" to use real APIs
NEXT_PUBLIC_USE_MOCK_DATA=true

# OpenAI Configuration (required for real API mode)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Supabase Configuration (required for real API mode)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Development Notes:
# - In mock mode (NEXT_PUBLIC_USE_MOCK_DATA=true), OpenAI and Supabase are not required
# - In real API mode (NEXT_PUBLIC_USE_MOCK_DATA=false), you must provide all credentials
# - Never commit .env files to version control
# - Use Netlify's environment variable UI for production deployments
